{
  "name": "@project-template/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx --env-file=../../.env watch src/worker.ts",
    "start": "node dist/worker.js",
    "build": "tsup",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@project-template/agent": "workspace:*",
    "@project-template/logger": "workspace:*",
    "@project-template/shared": "workspace:*",
    "bullmq": "^5.79.1",
    "pino": "^10.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
