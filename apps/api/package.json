{
  "name": "@project-template/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsup src/server.ts --format esm",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@project-template/agent": "workspace:*",
    "@project-template/db": "workspace:*",
    "@project-template/logger": "workspace:*",
    "@project-template/shared": "workspace:*",
    "bullmq": "^5.79.1",
    "fastify": "^5.8.5",
    "ioredis": "^5.11.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
